# API_Gateway runtime dependencies
# Canonical entrypoint: api_gateway_v2.py (run via start_gateway.sh).
# Direct imports: fastapi, uvicorn, httpx. pydantic is FastAPI's core
# runtime dependency. Pins are lower bounds matching the verified env.
fastapi>=0.135.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.12.0
